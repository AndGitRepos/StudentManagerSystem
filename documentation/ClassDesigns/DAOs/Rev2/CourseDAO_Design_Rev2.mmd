classDiagram
    class CourseDAO {
        -CourseDAO()
        +addCourse(course)
        +findById(id)
        +findByName(name)
        +findAll()
        +update(course)
        +delete(id)

        -getAllCoursesFromResultSet()
        -getCourseFromResultSet()
    }